#!/usr/bin/env bash
# wget install — profile nodes. Generated by vibed-infra package.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"
PACKAGER_RAW="${PACKAGER_RAW:-https://raw.githubusercontent.com/naiemk/vibed-infra/main}"
export PACKAGECONFIG_URL="${PACKAGECONFIG_URL:-$SCRIPT_DIR/packageconfig.yaml}"
export PRODUCT_RAW="${PRODUCT_RAW:-$SCRIPT_DIR}"
export INFRA_PROFILE=nodes
export INSTALL_DIR="${INSTALL_DIR:-.}"
if [[ "$PACKAGER_RAW" =~ ^/ ]]; then
  exec bash "$PACKAGER_RAW/install.sh" --profile nodes
fi
if command -v curl >/dev/null 2>&1; then
  exec bash <(curl -fsSL "${PACKAGER_RAW}/install.sh") --profile nodes
else
  exec bash <(wget -qO- "${PACKAGER_RAW}/install.sh") --profile nodes
fi
